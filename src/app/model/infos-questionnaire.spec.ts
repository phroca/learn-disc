import { InfosQuestionnaire } from './infos-questionnaire';

describe('InfosQuestionnaire', () => {
  it('should create an instance', () => {
    expect(new InfosQuestionnaire()).toBeTruthy();
  });
});
